// Reserved for the floating window slice.
// Approved future window types: Cost Detail, Context Detail, Queue Item Detail.
// Prop contract: FloatingWindowLayerProps in baseUxPresentationTypes.ts.
// Do not implement drag, resize, stacking, z-order, or float UI here until the float slice begins.

import type { FloatingWindowLayerProps } from '../baseUxPresentationTypes';

export function FloatingWindowLayer(_props: FloatingWindowLayerProps): null {
  return null;
}
